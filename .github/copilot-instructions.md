# GitHub Copilot Instructions

本仓库的统一 Agent 规则位于根目录 `AGENTS.md`。

开始工作前必须依次读取：

1. `AGENTS.md`
2. `.ai/ACTIVE_TASK.md`
3. 若存在 active change，读取对应 `openspec/changes/<change-id>/`
4. `openspec/project.md` 与相关 baseline spec

必须使用简体中文与用户沟通。不要在本文件维护另一套范围、架构、任务或验证规则；若本文件与 `AGENTS.md` 冲突，以 `AGENTS.md` 为准。
